/*

var num = prompt("What number do you want to start with?");
var originalNumber = num;
var exponent = 2;

while(exponent <+16) {
    num = num * originalNumber
    console.log(originalNumber + " to the power of " exponent " = " + num);
    exponent = exponent + 1;
}

var increase = 1;
while(increase <65536) {m
    increase = increase * 2;
    console.log(increase);
}*/

/*var number = Math.floor(Math.random() * 6) + 1;
var guess;
do {
    guess = ParseInt(prompt("Enter a number between 1 and 6"));
} while(guess !== number);

alert("Your guess of " + guess + " matches the number " + number + "!");*/

/*var allCones = Math.floor(Math.random() * 50) + 50;

do {
    var conesBought = Math.floor(Math.random() * 5) + 1;
    console.log(conesBought + "cones sold");

    if(conesBought >= allCones) {
        console.log("I can't sell you " + conesBought + " cones, I only have " + allCones);
    } " cones I only have " + allCones;
} while (allCones >0);
console.log("Yay! I sold them all!");
    // do another if statement to say you sold them all?*/

var count = 0;
while (count <= 100) {
    count = count +1
    console.log(count);
}

for(count = 0; count <= 100; count = count + 1){
    console.log(count);
}

for (i = 0; i<=100; i++){
    console.log(i);
}
